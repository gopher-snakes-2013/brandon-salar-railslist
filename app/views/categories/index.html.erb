<h1>Railslist Home Page</h1>
<h3>Here are all our categories!</h3>
<div>
  <% @categories.each do |category| %>

    <span><%= link_to category.name, category_path(category.id) %></span></br>
  <% end %>
</div>